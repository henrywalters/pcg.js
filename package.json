{
  "name": "pcg",
  "version": "1.0.0",
  "description": "Client Side Library for Product Code Generator",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "find src/ | entr tsc",
    "demo-serve": "node demo/server.js",
    "demo-build": "cd demo && webpack && cd ..",
    "demo-watch": "cd demo && webpack --watch && cd .."
  },
  "author": "Henry Walters",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.3",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "hcore": "^1.2.0"
  }
}
